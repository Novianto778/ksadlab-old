export enum SubmoduleType {
    VIDEO = 'video',
    PDF = 'pdf',
    RESOURCE = 'resource',
}

export enum MemberProgressStatus {
    IN_PROGRESS = 'in_progress',
    COMPLETED = 'completed',
}

export const MAX_LEVEL = 4;
